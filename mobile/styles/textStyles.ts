import { StyleSheet } from "react-native";

export const textStyles = StyleSheet.create({
  sectionTitle: {
    color: "white",
    fontSize: 30,
    marginBottom: 10,
  },
  bodyText: {
    color: "white",
    fontSize: 18,
  },
});
