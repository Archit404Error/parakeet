import { StyleSheet } from "react-native";

export const viewStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
    gap: 20,
  },
  floatingBubbleView: {
    minWidth: "90%",
    backgroundColor: "gray",
    color: "white",
    borderRadius: 10,
    padding: 10,
  },
});
